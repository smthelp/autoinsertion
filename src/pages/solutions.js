import { createLayout, pageHero, icon } from './layout';
const steps=['PCB Loader','S4000 Axial','S3010A Radial','S7020T Terminal','S7900 Odd Form','DIP AOI','S-WS450 Wave','Post-solder AOI','PCB Unloader'];
export async function solutionsPage(){const content=`${pageHero('Complete line solutions','One flow. Every insertion and transfer point engineered.','A turnkey concept connects component preparation, insertion, inspection, soldering and board handling around your actual process.','<a class="btn btn-primary" href="/contact">Request a line concept</a>')}<section class="section"><div class="shell"><div class="section-head"><div><span class="eyebrow">Reference flow</span><h2>From bare board to soldered assembly.</h2></div><p>This is a configurable reference sequence, not a fixed prescription. Unneeded processes are removed after application review.</p></div><div class="flow">${steps.map((s,i)=>`<div class="flow-step"><span>0${i+1}</span><b>${s}</b></div>${i<steps.length-1?`<div class="flow-arrow">${icon('chevron')}</div>`:''}`).join('')}</div></div></section><section class="section section-alt"><div class="shell grid grid-3"><article class="card"><div class="card-body"><div class="feature-icon">${icon('check')}</div><h3>Application validation</h3><p>Component dimensions, packaging, insertion force, board support and clinch requirements.</p></div></article><article class="card"><div class="card-body"><div class="feature-icon">${icon('check')}</div><h3>Interface planning</h3><p>Board direction, conveyor height, SMEMA-style signals and upstream/downstream handoff confirmed case by case.</p></div></article><article class="card"><div class="card-body"><div class="feature-icon">${icon('check')}</div><h3>Acceptance criteria</h3><p>Define sample boards, target cycle, quality checks, training and FAT evidence before shipment.</p></div></article></div></section><section class="section section-dark"><div class="shell"><div class="section-head"><div><span class="eyebrow">Open integration</span><h2>Fit the new process into the factory you already operate.</h2></div></div><p style="max-width:760px;color:#c7d5dc">Existing Panasonic, JUKI and Universal Instruments environments can be reviewed at the protocol and conveyor-interface level. Compatibility is never assumed: Southern Machinery engineering confirms signals, dimensions and responsibility boundaries before quotation.</p></div></section>`;return new Response(createLayout('Complete THT Auto Insertion Lines',content,'','Configure a turnkey THT insertion line with axial, radial, terminal, odd-form, AOI, wave soldering and board handling.'),{headers:{'Content-Type':'text/html;charset=UTF-8'}})}
